// in general, use console.log for debugging
// remove console.log after debugging
// to add logs that are really meant to be logged and not for debugging, use this function
// this is so that these non-debugging logs can be easily turned off when needed
// console should be kept clean most of the time
function log(str) {
    console.log(str);
}